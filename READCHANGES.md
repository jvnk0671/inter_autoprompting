тут что я добавил + с некой ориентировкай на константина которому предстоит работа с фронтом
сначала в общих чертах: я добавил оченщика промтов и доп мод на любой из методов который
 переводит на англ оптимизит выбранным методом и обратно на исходный язык
еще бенчмарк.пу это скрипт для сравнения всех методом это не нужно в самом сайте но скорее всего понадобится на защите

часть про то что именно я изменил и то что добавить нада (там немного совсем) это именно для фронта

DONE

1.новый формат Request (POST /optimize)
к отправляемому JSON добавились два новых необязательных поля (по умолчанию false).
также теперь корректно обрабатывает ch_lim и method.
{
  "prompt": "Текст...",
  "method": "my_promptomatix",   // одно из "my_promptomatix", "coolprompt", "promptomatix"
  "ch_lim": 2000,         
  "target_model": "...",
  "system_model": "...",
  
  // НОВЫЕ ПОЛЯ:
  "evaluate": false,  // true = запустить оценку качества промпта (занимает +10 сек)
  "translate": false  // true = перевести на англ -> улучшить -> перевести обратно
}

2.новый формат Response
если фронт передал "evaluate": true, бэкенд вернет две новые метрики (float от 0.0 до 1.0) если false — вернется null
{
  "optimized_prompt": "Улучшенный текст...",
  "init_tokens": 15,
  "final_tokens": 145,
  то что добавлено 
  "init_score": 0.45,   // оценка дп улучшения
  "final_score": 0.95   // оценка после улучшения
}

3.что нужно добавить в UI:
Два чекбокса (или свитча) перед кнопкой отправки:
"глубокая оптимизация (English Boost)" -> управляет полем translate.
"оценить качество (LLM Judge)" -> управляет полем evaluate.
отображение метрик: Если бэкенд вернул init_score и final_score (не null)
вывести их под результатом

P.S надоели ошбки с перегрузками бесплатных моделей так что заменил их на нормальные поэтому вводите мой ключ